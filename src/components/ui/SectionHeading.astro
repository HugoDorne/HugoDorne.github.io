---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<div class="mb-8 flex items-center gap-4">
	<h2 class="text-2xl font-bold text-zinc-50 md:text-3xl">{title}</h2>
	<div class="h-px flex-1 bg-zinc-800"></div>
</div>
